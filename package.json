{
  "name": "marol",
  "private": true,
  "version": "0.9.0",
  "license": "Apache-2.0",
  "scripts": {
    "setup": "npm --prefix sidecar install && npm --prefix ui install && npm run build:sidecar",
    "build:sidecar": "npm --prefix sidecar run build",
    "dev": "npm run build:sidecar && cargo tauri dev --config src-tauri/tauri.conf.json",
    "smoke": "npm --prefix sidecar run build && node sidecar/smoke.mjs",
    "test:rust": "cd src-tauri && cargo test",
    "tauri": "tauri",
    "build:app": "npm --prefix ui ci && npm run tauri -- build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
